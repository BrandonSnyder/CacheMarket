{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "blah": "concurrently \"cd server && npm run start\" \"cd client && npm start\"",
    "seed": "node server/seeds/seed.js",
    "watch": "nodemon server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "build:ui": "cd ../react-front && npm install && npm run build",
    "deploy": "git push heroku master",
    "start": "npm run build:ui && git add . && git commit -m uibuild && npm run deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server-express": "^3.8.0",
    "bootstrap": "^5.1.3",
    "concurrently": "^7.2.1",
    "dark-mode-light-mode": "^1.3.3",
    "express": "^4.17.1",
    "graphql": "^15.5.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.11.6",
    "react-bootstrap": "^2.4.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  },
  
    "engines": {
      "node": "v16.14.2"
    }
  
}
